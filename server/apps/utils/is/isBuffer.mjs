/**
 * *****************************************************************************
 *
 *
 * *****************************************************************************
 */

export const isBuffer = buf => {
  return buf && buf.buffer && buf.buffer instanceof ArrayBuffer;
}
