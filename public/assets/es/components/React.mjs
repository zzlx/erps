/**
 * *****************************************************************************
 * 
 * React
 *
 * 注意：无法在浏览器客户端环境中执行
 *
 * *****************************************************************************
 */

let React = globalThis.React;

try {
  if (React == null) {
    React = await import('react');
  }
} catch (e) {
  //
}

export { React };
