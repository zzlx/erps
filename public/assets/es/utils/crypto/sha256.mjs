/**
 * *****************************************************************************
 *
 *
 * *****************************************************************************
 */

import { SHA256 } from './SHA256.mjs';

export const sha256 = msg => new SHA256().update(msg); 

console.log(sha256('w'));
