/**
 * *****************************************************************************
 *
 *
 * *****************************************************************************
 */

export function isPromise (v) {
  return Boolean(v && typeof v === 'object' && typeof v.then === 'function');
}
