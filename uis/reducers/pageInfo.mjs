/**
 * *****************************************************************************
 *
 *
 * *****************************************************************************
 */

const defaultSettings = {
  title: 'HomePage',
  description: '',
  viewport: "width=device-width, initial-scale=1.0, shrink-to-fit=no",
  keywords: 'HomePage',
}

export const pageInfo = (state = defaultSettings, action) => {
  return state;
}
