/**
 * *****************************************************************************
 *
 * profiles 配置文件
 *
 * *****************************************************************************
 */

const initialState = {
  pathname: '/',
};

export const location = function (state = initialState, action) {
  switch(action.type){
    default: 
    return state;
  }
}
